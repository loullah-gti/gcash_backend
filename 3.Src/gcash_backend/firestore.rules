rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read: if
          request.auth.uid!=null;
    }
    match /users/{uid}/para {
    	allow read, write: if request.auth.uid == uid;
    }
    
    match /cartes/{carteId} {
      allow read, write: if resource.data.etat==1 && request.auth.uid == request.resource.data.usedById;
    }
  }
}